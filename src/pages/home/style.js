import {Box,  Grid, styled} from "@mui/material";


export const WrapperHomePage = styled (Box)({
    display:'flex',
    flexDirection:'column',
    position:'relative',
    gap:'5px',
    padding:'24px'
})
export const WrapperCards=styled(Grid)({

})