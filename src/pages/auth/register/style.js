import { styled } from "@mui/material/styles";
import { Typography } from "@mui/material";

export const IncitingText = styled(Typography)({
    color: '#1900D5',
    marginLeft: '5px',
    cursor: 'pointer',
    display: 'inline'  // Ensure the component can be styled as an inline element
});
