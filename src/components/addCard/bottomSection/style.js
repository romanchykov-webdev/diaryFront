import {Box, styled} from "@mui/material";

export const WrapperBottom = styled(Box)({
    display: 'flex',
    // outline:'1px solid red',
    justifyContent: 'space-between',
    alignItems: 'center',
})

export const WrapperCreateCard = styled(Box)({
    cursor: 'pointer',
})
export const WrapperExit = styled(Box)({
    cursor: 'pointer',
})