import React from 'react';
// import {useDispatch} from "react-redux";

const PopUpLesson = () => {
    // const dispatch = useDispatch();
    // const toggle=useSelector((store)=>store.auth.user.popupForNewUser);



    return (
        <div>
            popup  popupForNewUser

        </div>
    );
};

export default PopUpLesson;